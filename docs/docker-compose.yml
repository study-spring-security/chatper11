version: "3.7"

services:
  mysql:
    image: mysql:8.0
    container_name: local-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      TZ: Asia/Seoul